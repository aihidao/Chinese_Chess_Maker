if (async_load[? "id"] == tcp_socket) {
    var client_socket = async_load[? "socket"];
    var ip            = async_load[? "ip"];
    var port          = async_load[? "port"];
    
    //server_disconnect_client(client_socket);
}
